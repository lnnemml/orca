# Runtime dependencies for the OrcaStudio sidecar.
# cclib / ase are added in later phases.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.0
# Structure generation (Phase 2.2): SMILES → 3D via ETKDGv3 + MMFF. The modern
# PyPI wheel is named `rdkit` (the old `rdkit-pypi` fork is deprecated).
rdkit>=2024.3
